import React from 'react';
import './InputButton.css';

function InputButton(props) {
    let dupa = function() {
        //
    }

    return <div>
    <button onClick = {dupa} className="inputButton">Add to list</button>
    </div>
}

export default InputButton;